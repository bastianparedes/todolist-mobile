import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    marginRight: 5,
    marginLeft: 5,
    width: '100%',
    display: 'flex',
    gap: 20,
    paddingBottom: 30
  }
});

export default styles;
