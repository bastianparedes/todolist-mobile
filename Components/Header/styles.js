import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    marginLeft: 10,
    marginTop: 40,
    flexDirection: 'row',
    gap: 20
  },
  text: {
    fontSize: 30
  },
});

export default styles;
